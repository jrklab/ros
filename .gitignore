# colcon workspaces (sources are cloned by each project's build.sh)
*/ws/

# colcon artifacts, wherever they land
build/
install/
log/

# python bytecode (ros2 launch compiles launch files in place)
__pycache__/
*.py[cod]

# local scratch
*.pcap
*.bag
*.log
